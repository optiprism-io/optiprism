name: docker

on: pull_request

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: docker context create mycontext
      - run: docker context use mycontext
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Build
        uses: docker/build-push-action@v3
        with:
          context: .
          file: docker/ui.dev.Dockerfile
          tags: eu.gcr.io/optiprism/ui:${{ github.head_ref }}

      - run: docker images
      - name: Push
        uses: mattes/gce-docker-push-action@v1
        with:
          creds: ${{ secrets.GCP_CONTAINER_SERVICE_ACCOUNT }}
          src: eu.gcr.io/optiprism/ui:${{ github.head_ref }}
          dst: eu.gcr.io/optiprism/ui:${{ github.head_ref }}